from django.conf.urls import patterns, url

# from .views import IndexView

__author__ = 'eraldo'

urlpatterns = patterns(
    '',
    # url(r'^$',
    #     IndexView.as_view(),
    #     name='index'),
)
